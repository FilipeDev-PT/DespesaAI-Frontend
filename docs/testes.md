# Testes — Frontend

```bash
npm test
npm run test:e2e
```

Unitários: schemas auth/cards, lógica mês vs período (`filters.test.ts`).

Playwright: `e2e/login.spec.ts` — formulário de login visível.

Instale browsers: `npx playwright install`.
